name: Build & Release APK

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.github/workflows/check.yml'
      - 'signing-fingerprint.txt'
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4

      # versionName becomes 1.0.<run>, versionCode the run number, so every push produces a
      # newer build that upgrades in place.
      - name: Stamp version
        id: ver
        run: |
          RUN=${{ github.run_number }}
          BASE=$(grep 'versionName' app/build.gradle.kts | head -1 | sed -E 's/.*"([0-9]+\.[0-9]+).*"/\1/')
          VERSION="${BASE}.${RUN}"
          sed -i -E "s/versionCode = .*/versionCode = ${RUN}/" app/build.gradle.kts
          sed -i -E "s/versionName = \".*\"/versionName = \"${VERSION}\"/" app/build.gradle.kts
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          grep -E 'versionCode|versionName' app/build.gradle.kts

      # The origin wall, the QR parser and the exit gesture are plain JVM code. A hole in the wall
      # is invisible in a screenshot, so the tests gate the build.
      - name: Unit tests
        run: ./gradlew :app:testDebugUnitTest --stacktrace

      - name: Build APKs
        env:
          REPORT_TOKEN: ${{ secrets.REPORT_TOKEN }}
        run: ./gradlew :app:assembleDebug :app:assembleRelease --stacktrace

      # A release with no tracker key collects reports on the phone and sends none. Say so in the
      # log rather than finding out from an empty tracker.
      - name: Report key present
        run: |
          if [ -z "${{ secrets.REPORT_TOKEN }}" ]; then echo "::warning::REPORT_TOKEN is not set; shake-to-report will queue on the phone and send nothing"; else echo "REPORT_TOKEN present"; fi

      - name: Collect APK
        run: |
          V=${{ steps.ver.outputs.version }}
          mkdir -p out debugout
          cp app/build/outputs/apk/release/app-release.apk "out/WebTools-v$V.apk"
          cp app/build/outputs/apk/debug/app-debug.apk "debugout/WebTools-v$V-debug.apk"
          ls -la out debugout

      # Android identifies an app by (packageName, signing cert). If the cert ever changes,
      # Obtainium updates fail with an opaque "Failure: Invalid", so pin it.
      - name: Verify signing certificate
        run: |
          APKSIGNER=$(find "$ANDROID_HOME/build-tools" -name apksigner | sort -V | tail -1)
          ACTUAL=$("$APKSIGNER" verify --print-certs out/*.apk \
            | grep -i 'SHA-256 digest' | head -1 | sed -E 's/.*[Dd]igest:[[:space:]]*//' | tr -d ' ')
          EXPECTED=$(tr -d ' :\n' < signing-fingerprint.txt | tr 'A-F' 'a-f')
          echo "expected $EXPECTED"
          echo "actual   $ACTUAL"
          [ "$ACTUAL" = "$EXPECTED" ] || { echo '::error::signing certificate drifted'; exit 1; }

      # AGP shortens resource paths in release builds, so ask the package what its icon is
      # instead of grepping filenames — the same lookup Obtainium and the launcher do.
      - name: Verify launcher icon is present
        run: |
          AAPT2=$(find "$ANDROID_HOME/build-tools" -name aapt2 | sort -V | tail -1)
          "$AAPT2" dump badging out/*.apk > badging.txt
          grep -E "^(application:|application-icon-)" badging.txt
          grep -qE "^application:.*icon='[^']+'" badging.txt \
            || { echo '::error::package declares no launcher icon'; exit 1; }

      - name: Upload release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: WebTools-apk
          path: out/*.apk
          if-no-files-found: error

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: WebTools-debug-apk
          path: debugout/*.apk
          if-no-files-found: error

      # One asset per release: Obtainium picks whatever it finds, and a debug APK alongside
      # the release one is how it ends up installing the wrong signature.
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.ver.outputs.version }}
          name: WebTools v${{ steps.ver.outputs.version }}
          files: out/WebTools-v${{ steps.ver.outputs.version }}.apk
          # Hand-written notes for the release, with the commit list appended under them.
          # RELEASE_NOTES.md describes the current minor; rewrite it when the minor changes.
          body_path: RELEASE_NOTES.md
          generate_release_notes: true
          make_latest: true

      # Tell the BrightMarket index a release exists instead of waiting for its cron. Soft
      # failure: a brand-new repo has no INDEX_DISPATCH_TOKEN yet, and the hourly cron catches
      # up regardless.
      - name: Ping the index
        if: success()
        continue-on-error: true
        run: |
          code=$(curl -s -o /dev/null -w '%{http_code}' -X POST \
            -H "Authorization: Bearer ${{ secrets.INDEX_DISPATCH_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/gi-os/brightmarket-index/dispatches \
            -d '{"event_type":"app-released"}')
          if [ "$code" = "204" ]; then echo "index pinged"; else echo "::warning::index ping refused with HTTP $code; the hourly cron will pick this up"; fi
